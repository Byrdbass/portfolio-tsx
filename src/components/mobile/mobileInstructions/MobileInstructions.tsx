import React, { JSX } from "react";
import '../phone.css'


const MobileInstructions: React.FC<{isActive: 'active' | ''}> = ({isActive}): JSX.Element => {
    return(
        <div>
            
        </div>

    )
}